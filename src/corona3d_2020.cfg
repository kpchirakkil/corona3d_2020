#################################################################
## corona3d_2020 parameter input file                          ##
##                                                             ##
## This file must be named "corona3d_2020.cfg" and reside in   ##
## the same directory as the corona3d_2020 executable file.    ##
## Blank lines or lines beginning with '#' are ignored.        ##
## The expected format for a parameter on a given line is:     ##
##                                                             ##
## ParameterName (some spaces or tabs) ParameterValue          ##
##                                                             ##
## Any additional columns following the first two are ignored. ##
#################################################################

num_testparts   100000       #number of test particles to track
part_type       H           #particle type to be tracked (H, O, N2, CO, or CO2)
dist_type       Hot_H       #initial distribution type (Hot_O, Hot_H, MB, or Import (must set import file paths below))
#pos_infile      pos.in      #filename of positions to import (3-column file of x, y, z coordinates in cm)
#vel_infile      vel.in      #filename of velocities to import (3-column file of vx, vy, vz values in cm/s)
timesteps       1000000     #number of total timesteps in simulation
dt              0.05        #size of timesteps (seconds)
planet_mass     6.4185e26   #Mars mass (grams)
planet_radius   3.397e8     #Mars radius (centimeters)
#planet_mass     4.8675e27  #Venus mass (grams)
#planet_radius   6.0518e8   #Venus radius (centimeters)

#############################################################
# Atmospheric profile input options
#
# The column order of species in neutral_densities must match
# order of background species listed in background species
# options below.
#
# If no density and/or temp profiles available, then
# comment out those lines and set a ref_height and ref_temp.
# The ref_height and ref_temp will be used to calculate a
# simple density scale height to be used for collisions.
#############################################################

profile_bottom_alt    80e5     #altitude above surface (cm) where atmospheric profiles begin
profile_top_alt       700e5    #altitude above surface (cm) where atmospheric profiles end
ref_height            150e5    #altitude above surface (cm) to extrapolate densities from (if no density profiles available)
ref_temp              180.4    #background temperature (Kelvin) at ref_height
#temp_profile          /home/rodney/git/corona3d_2020/src/inputs/Venus/VenusTemp_LSA_FoxSung2001.csv
#neutral_densities     /home/rodney/git/corona3d_2020/src/inputs/Venus/bg_densities_LSA_FoxSung01.csv
temp_profile          /home/rodney/git/corona3d_2020/src/inputs/Mars/MarsTemp_Fox2014.csv
neutral_densities     /home/rodney/git/corona3d_2020/src/inputs/Mars/bg_densities_Fox2014.csv
ion_densities

#############################################################
# Background species options
#
# Possible background species are:  H, O, N2, CO, CO2
#
# Note that the order listed matters. This is the order
# in which the code will check for collisions at each
# timestep. Therefore, it is more efficient to list them
# in descending order of collision likelihood.
#############################################################

num_bgparts  4  #number of different background species to use

bg_part1_config    /home/rodney/git/corona3d_2020/src/inputs/O_Mars_HotH.cfg
bg_part2_config    /home/rodney/git/corona3d_2020/src/inputs/N2_Mars_HotH.cfg
bg_part3_config    /home/rodney/git/corona3d_2020/src/inputs/CO_Mars_HotH.cfg
bg_part4_config    /home/rodney/git/corona3d_2020/src/inputs/CO2_Mars_HotH.cfg


#########################################################
# Output options
#########################################################

output_dir   /home/rodney/Documents/coronaTest/outdata/     #default output directory (comment out to use same directory as executable)

num_traced         10       #number of random test particles to output detailed trace data on
output_pos_freq   1000       #number of timesteps between outputting all active particle positions (set to zero to never output position data)
#output_pos_dir    ./      #comment out to use default output directory above
shell_bottom      395e5     #(cm of altitude) bottom of shell to gather simulation stats from
shell_top         400e5     #(cm of altitude) top of shell to gather simulation stats from
#shell_output_dir   ./      #comment out to use default output directory above
